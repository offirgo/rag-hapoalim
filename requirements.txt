# =============================================================================
# RAG SYSTEM REQUIREMENTS
# =============================================================================

# -----------------------------------------------------------------------------
# CORE DOCUMENT PROCESSING
# Used by: ExcelProcessor, WordProcessor for reading and parsing documents
# -----------------------------------------------------------------------------
pandas>=2.0.0                    # Excel file processing and data manipulation
openpyxl>=3.1.0                  # Excel (.xlsx) file reading engine
python-docx>=0.8.11              # Word (.docx) file processing

# -----------------------------------------------------------------------------
# EMBEDDING GENERATION (LOCAL)
# Used by: SentenceTransformersEmbedder for converting text to vectors
# Note: First install downloads ~1.5GB PyTorch dependencies
# Note: First model use downloads model files (~23-400MB depending on model)
# -----------------------------------------------------------------------------
sentence-transformers>=2.2.0     # Local text embedding models (includes torch, transformers)
numpy>=1.24.0                    # Array operations for embeddings
scikit-learn>=1.3.0              # ML utilities (used by sentence-transformers)

# -----------------------------------------------------------------------------
# VECTOR STORAGE AND SEARCH
# Used by: Future VectorStore implementation for similarity search
# -----------------------------------------------------------------------------
faiss-cpu>=1.7.4                 # Facebook AI Similarity Search (CPU version)

# -----------------------------------------------------------------------------
# DATA VALIDATION AND CONFIGURATION
# Used by: Pydantic models for type validation and configuration management
# -----------------------------------------------------------------------------
pydantic>=2.0.0                  # Data validation and settings management
python-dotenv>=1.0.0             # Environment variable management (.env files)

# -----------------------------------------------------------------------------
# API FRAMEWORK (FUTURE)
# Used by: Future REST API endpoints for RAG system
# -----------------------------------------------------------------------------
fastapi>=0.100.0                 # Modern web framework for APIs
uvicorn[standard]>=0.23.0        # ASGI server for running FastAPI

# -----------------------------------------------------------------------------
# OPTIONAL: CLOUD LLM INTEGRATION
# Used by: Future OpenAI integration for higher-quality embeddings/generation
# Note: Requires API key and costs money per request
# TODO: Add support for other providers (Anthropic, Cohere, etc.)
# -----------------------------------------------------------------------------
openai>=1.0.0                    # OpenAI API client (optional)

# -----------------------------------------------------------------------------
# DEVELOPMENT AND TESTING
# Used by: Running tests, development workflow
# -----------------------------------------------------------------------------
pytest>=7.4.0                    # Testing framework
pytest-asyncio>=0.21.0           # Async testing support
pytest-cov>=4.0.0                # Test coverage reporting

# -----------------------------------------------------------------------------
# OPTIONAL: PERFORMANCE MONITORING
# Used by: Monitoring embedding performance, memory usage (development)
# -----------------------------------------------------------------------------
# psutil>=5.9.0                  # System monitoring (uncomment if needed)
# memory-profiler>=0.60.0        # Memory usage profiling (uncomment if needed)